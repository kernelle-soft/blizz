use anyhow::Result;

pub async fn handle() -> Result<()> {
  bentley::info("Refresh functionality not yet implemented");
  bentley::info("This will clean and re-download session data when completed");
  Ok(())
}
