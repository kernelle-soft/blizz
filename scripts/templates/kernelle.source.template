#!/bin/sh
# Kernelle Cursor Workflows Integration

# Please do not remove or move these lines. It is used to do manage internal setup for kernelle.
KERNELLE_HOME="${KERNELLE_HOME:-$HOME/.kernelle}"
source "$KERNELLE_HOME/kernelle.internal.source"